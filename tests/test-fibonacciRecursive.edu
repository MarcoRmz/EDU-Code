start

function int fiboR(int x) {
	if (x == 0) {
		return 0
	} elseif (x == 1){
		return 1
	}

	return fiboR(x-1) + fiboR(x-2)
}

main{
	int x

	x = input("Calculate fibonacci for: ")

	x = fiboR(x)
	print("Fibonacci rec: " + x)
}

end
